export type LanguageType = "en" | "pt";
export type DonateType = "one-time" | "subscription";

export const HeroHeightVariants = {
  "1/2": "h-[50dvh]",
  "3/4": "h-[75dvh]",
  full: "h-[100dvh]",
} as const;
